Para hacer la práctica he tenido que crear estas bases de datos que no teniamos hecha en la de empresa_asir1

CREATE DATABASE IF NOT EXISTS concesionario;
USE concesionario;

CREATE TABLE IF NOT EXISTS coches (
    id INT AUTO_INCREMENT PRIMARY KEY,
    marca VARCHAR(100) NOT NULL,
    anio INT NOT NULL,
    color VARCHAR(50) NOT NULL
);

He usado el usuario que ya tenia creado, el root.


